# 📖 **Kore User Manual**

Welcome to Kore! This manual will guide you through using the terminal-based DAW.

## 🎯 **Quick Start**:

- Run the build instructions in README.md
- Use the arrow keys to navigate
- Press `C`on a track to create a clip
- Press `Space` on the play button (▶) to start playback
- Press `F2` to render your project to a WAV file
- Press `F1` to quit

<br>

## 🖥️ **Interface Overview**:

### **Header Bar**:

<img width="750" height="154" alt="grafik" src="https://github.com/user-attachments/assets/5d5ba91f-866d-4474-a251-0f5c937080b2" />


- `Play/Pause`: Control playback (currently UI placeholders)
- `Quit: F1`: Exit the application
- `File: F2`: Open file menu for rendering and loading

### **Playlist View**:

<img width="2844" height="337" alt="grafik" src="https://github.com/user-attachments/assets/0ec60330-6a79-422c-862b-3d7cec6cfd74" />


- `Track Label`: Shows track number
- `◉ Indicator`: Green when track is active/unmuted, gray when muted
- `Timeline`: Visual representation of clips on the track
- - `/` = Clip start
- - `=` Clip body
- - `\` = Clip end

<br>

## ⌨️ **Keyboard Navigation**:

`↑ ↓ ← →`: Move cursor
`Space`: Select/Activate item under cursor
`Delete`: Delete item under cursor
`F1`: Quit
`F2`: File Menu
`ESC`: Cancel Dialogue
